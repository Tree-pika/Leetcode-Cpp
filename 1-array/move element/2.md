# 双指针法
## 快慢指针法
* 通过一个快指针和慢指针在一个for循环下完成两个for循环的工作。
* 定义快慢指针
  * 快指针：寻找新数组的元素 ，新数组就是不含有目标元素的数组
  * 慢指针：指向更新 新数组下标的位置
* 运用点：
  * 移除数组中的特定元素（与给定值比较）
  * 移除数组的重复元素（与前一个元素比较：即此时慢指针指向的元素）
## 双指针
* 运用点
  * 排序：将两个分别排好序的数组，merge成新的排序数组；两个指针分别指向自己的数组，然后依次比较，自己这方更大/小，被挪到新数组后，就更新指针，否则不动，继续参与下一轮比较。